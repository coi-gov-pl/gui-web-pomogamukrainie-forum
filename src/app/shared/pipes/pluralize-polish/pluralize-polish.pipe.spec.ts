import { PluralizePolishPipe } from './pluralize-polish.pipe';

describe('PluralizePolishPipe', () => {
  it('create an instance', () => {
    const pipe = new PluralizePolishPipe();
    expect(pipe).toBeTruthy();
  });
});
